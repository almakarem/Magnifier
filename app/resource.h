// app/resource.h
// Numeric resource IDs shared between Magnifier.rc and the C++ code that
// calls LoadIcon / LoadImage. Keep the values stable - they are baked into
// the .exe resource table and the runtime looks them up by number.
#pragma once

#define IDI_APP 101
